import {createContext} from "react";

export const MainFrameContext = createContext(null);
export const IntermediateFrameContext = createContext(null);
export const BasicFrameContext = createContext(null);
