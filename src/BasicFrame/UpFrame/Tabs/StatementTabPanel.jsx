import {Box} from "@mui/material";

const StatementTabPanel = ({updateEditedBasicLine, clear, lineType}) => {
    return (
        <Box
            sx={{
                boxSizing: 'border-box',
                display: 'flex',
                flexDirection: 'column',
                width: '100%',
                height: '100%',
            }}
        >
        </Box>
    );
}
export default StatementTabPanel;