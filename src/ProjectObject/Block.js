class Block {
    constructor(type, title, enTitle, code) {
        this.type = type;
        this.title = title;
        this.enTitle = enTitle;
        this.code = code;
    }
}

export default Block;